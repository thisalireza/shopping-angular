import { ImageLoadDirective } from './lazy-load.directive';

describe('ImageLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadDirective();
    expect(directive).toBeTruthy();
  });
});
