import { CaptchaInputDirective } from './captcha-input.directive';

describe('CaptchaInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CaptchaInputDirective();
    expect(directive).toBeTruthy();
  });
});
